tarkov.mooo.com {
    # Proxy inverso hacia el contenedor de la app
    reverse_proxy app:3000

    # Logs de acceso (opcional, comenta si no los necesitas)
    log {
        output file /data/logs/access.log {
            roll_size 10mb
            roll_keep 5
        }
    }
}
